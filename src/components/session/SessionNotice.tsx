import React from "react";
import SessionNotification, { SessionNotificationProps } from "./SessionNotification";

export default function SessionNotice(props: SessionNotificationProps) {
  return <SessionNotification {...props} />;
}

